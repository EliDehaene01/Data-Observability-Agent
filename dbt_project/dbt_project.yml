name: 'data_observability_agent'
version: '1.0.0'
config-version: 2

profile: 'data_observability_agent'

model-paths: ["models"]
test-paths: ["tests"]
macro-paths: ["macros"]

target-path: "target"
clean-targets:
  - "target"
  - "dbt_packages"

models:
  data_observability_agent:
    landing:
      +materialized: view
    prep:
      +materialized: view
    serve:
      +materialized: table
